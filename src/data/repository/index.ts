import * as customerRepository from './customer';
import * as voucherRepository from './/voucher';
import * as voucherReservationRepository from './voucher-reservation';

export {
    customerRepository,
    voucherRepository,
    voucherReservationRepository,
};
